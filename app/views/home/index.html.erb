<h1>Home#index</h1>
<p>Find me in app/views/home/index.html.erb</p>

<% @categories.each do |category| %>
  <p>
    <%= category.categoryname %>
    <%= link_to category.categoryname, category_path(category) %>
  </p>
<% end %>
