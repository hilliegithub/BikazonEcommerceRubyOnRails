<h1>Categories#show</h1>
<p>
    <%= @category.categoryname %>
</p>

<%= paginate @products %>
<% @products.each do |product| %>
    <p><%= link_to product.productname, product_path(product) %></p>
<% end %>